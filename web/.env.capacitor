VITE_API_BASE_URL=https://alumni-watch-v1dg.onrender.com
# RevenueCat's iOS "public app-specific API key" (Project settings > API keys
# in the RevenueCat dashboard). It's meant to ship inside client apps --
# not a secret -- so it's fine committed here like the URL above.
VITE_REVENUECAT_API_KEY=
